```js
//  数组的解构赋值
const arr = [1, 2, 3]
let [a, b, c] = arr

//  对象
let { foo, bar } = { foo: 'abc', bar: '123' }
let { foo: f, bar: b } = { foo: 'abc', bar: '123' }

//  对象的多重解构
let { foo: [a, b] } = { foo: [1, 2] }

//  字符串的解构赋值
let [a] = 'miao' //m
```



```js
//  扩展运算符
const arr = [1, 2, 3]
Math.max(...arr) //  Math.max(1,2,3)

let obj3 = { ...obj1, ...obj2 }
```


